// Current ToS agreement version. Bump this whenever the terms change;
// a mismatch with config 'setup.tosAcceptedVersion' forces the setup
// wizard to reopen and the user to re-accept. Keep the version string in
// the ToS modal title in setup.html in sync.
module.exports = { TOS_VERSION: '0.11' };
